- fix: 덱 빌더 편성 제한 규칙 오판 수정
  - `N장만`, `N장까지` 문구를 일반 효과 텍스트에서 덱 제한으로 해석하지 않도록 보정
  - 기본 편성 제한을 `오시 1장 / 일반 4장 / 옐 총 20장`으로 유지
- fix: 무제한 편성 문구 판정 강화
  - `덱에 몇 장이라도 넣을 수 있다` 또는 `デッキに何枚でも入れられる` 문구일 때만 50장 허용
- fix: 저장된 덱의 과거 오판 제한값 자동 보정
  - 기존 덱 로드 시 `max_per_card`를 최신 규칙으로 재계산
