- fix: DB 있음에도 "DB파일이 존재하지 않습니다"로 표시되던 오탐 수정
  - iOS/Android `needsDbUpdate()`에서 prints 행 유무 체크 제거
  - DB 파일/스키마가 정상이면 데이터가 없어도 "DB 없음"으로 판정하지 않음
- fix: 최초 실행 시 DB가 없으면 수동 갱신을 기다리지 않고 자동 다운로드 시작
  - iOS/Android `bootstrap()`에서 missing 감지 시 자동 업데이트 트리거
- feat: 덱 빌더 레어리티 선택 기반 이미지/데이터 처리 개선
  - 선택 레어리티(`selectedRarity`) 직렬화/복원 및 이미지 노출 경로 정비
